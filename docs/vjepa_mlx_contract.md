# V-JEPA2 on MLX Contract

This file is generated by `tools/inspect_vjepa_video_contract.py`. It freezes the foundation contract before any full V-JEPA2 weight load or MLX checkpoint mapping.

## Target lock

- Target model id: `facebook/vjepa2-vitg-fpc64-256`
- TRIBE checkpoint: `facebook/tribev2` / `best.ckpt`
- Released TRIBE video feature dims: `[2, 1408]`
- Required final feature tensor order: `[layers, dim, time]`

## V-JEPA2 architecture fields

- Config status: `loaded`
- Hidden size: `1408`
- Frames per clip: `64`
- Tubelet size: `2`
- Image/crop size: `256`
- Patch size: `16`
- Hidden layers: `40`
- Attention heads: `22`
- MLP ratio: `4.363636363636363`
- Intermediate size: `6144`
- Activation: `gelu`
- QKV bias: `True`
- LayerNorm eps: `1e-06`
- Positional/ROPE details: `unknown_from_public_config`

## TRIBE aggregation contract

- Layers: `[0.5, 0.75, 1.0]`
- Cache n layers: `20`
- Layer aggregation: `group_mean`
- Token pooling: `mean`
- Frequency: `2.0` Hz
- Temporal resampling: neuralset extractor controlled; foundation records frequency only

## Dtype policy

- `reference`: float32 torch CPU/MPS when available
- `tiny_parity`: float32 torch CPU vs MLX default device
- `real_probe`: float32 is the primary correctness lane when memory preflight allows it
- `fp16` / `bf16`: optimization-only lanes; they must not replace the float32 correctness artifact unless separately finite and parity-proven against the float32 reference

## Foundation interpretation

Unknown positional/ROPE details do not block tiny MLX/PyTorch mechanics parity. They do block any claim of semantic V-JEPA2 parity and must be resolved before mapping/probe milestones.

## Ralph probe addendum (2026-05-14)

The full ViT-g MLX probe has now moved past foundation-only status:

- MLX runtime artifact: `cache_mac_m2_verification/mlx_vjepa_vitg/metadata.json`
- Feature artifact: `cache_mac_m2_verification/mlx_vjepa_vitg/features.npy`
- Produced shape: `[2, 1408, 1]`
- Finiteness: `true`
- Reference comparison: passed the recorded tolerance gate against `cache_mac_m2_verification/vjepa_reference/aggregated_features.npy` with cosine `0.9836995443` and mean absolute error `0.9371868716`.
- Runtime precision: float32. A prior fp16 runtime attempt completed shape-wise but produced non-finite values, so fp16 output must fail closed unless a future optimization proves finite parity.

This probe evidence is sufficient to proceed to the TRIBE precomputed-feature smoke gate. It is not final full success until TRIBE consumes these finite features and the c3 video smoke produces the expected prediction report.

## Ralph integration addendum (2026-05-14)

The c3 video TRIBE smoke gate now consumes the finite MLX ViT-g feature artifact:

- Smoke report: `cache_mac_m2_verification/mlx_vjepa_tribe_c3.json`
- Backend label: `mlx-vjepa2`
- Input features: `cache_mac_m2_verification/mlx_vjepa_vitg/features.npy`
- Video feature shape: `[2, 1408, 1]`
- Prediction shape: `[11, 20484]`
- Prediction finiteness: `true`

This completes the c3 video integration gate. The next unresolved plan gate is `c1_video_audio`, which must either complete with MLX video plus existing audio handling or fail with an audio-specific blocker rather than a video-feature blocker.

## Ralph audio-video addendum (2026-05-14)

The c1 audio+video smoke gate now completes with existing TRIBE audio handling and MLX ViT-g video features:

- Smoke report: `cache_mac_m2_verification/mlx_vjepa_tribe_c1_video_audio.json`
- Backend label for video: `mlx-vjepa2`
- Modalities used: `audio`, `video`
- Video feature shape: `[2, 1408, 1]`
- Prediction shape: `[11, 20484]`
- Prediction finiteness: `true`
- Video blocker status: `false`

This completes the c1 audio+video gate; any remaining work is notebook/report UX and final audit rather than V-JEPA2 video-feature feasibility.

## Ralph notebook addendum (2026-05-14)

The Mac notebook now exposes report-backed MLX V-JEPA2 modes:

- `safe`: bounded default notebook execution.
- `mlx_vjepa_probe`: reads the MLX ViT-g probe report plus c3/c1 TRIBE smoke reports and prints status, feature shape, finiteness, and parity without claiming tiny/non-ViT-g compatibility.
- `mlx_vjepa_predict`: guarded prediction mode using `tools/run_mlx_vjepa2_tribe_smoke.py`; it refuses to run unless `TRIBEV2_MLX_VJEPA_PREDICT_OPT_IN=1` and required probe artifacts exist.

Verification artifacts:

- `cache_mac_m2_verification/notebook_safe_executed.ipynb`
- `cache_mac_m2_verification/notebook_mlx_vjepa_probe_executed.ipynb`
- `cache_mac_m2_verification/notebook_mlx_vjepa_predict_refused_executed.ipynb`
- refusal report: `cache_mac_m2/mlx_vjepa_predict_reports/refused.json`

## Earlier Ralph final audit addendum (2026-05-14; superseded for dtype provenance)

Earlier final audit status: **approved complete for the original scoped Mac M2 run**, later superseded by the float32 correctness completion addendum below for dtype/reference provenance.

Evidence summary:

- Memory/mapping gates passed: `cache_mac_m2_verification/vjepa_vitg_memory_preflight.json`, `cache_mac_m2_verification/vjepa_mlx_weight_mapping.json`.
- Reference aggregation passed: `cache_mac_m2_verification/vjepa_reference/aggregated_features.npy` shape `[2, 1408, 1]`.
- Full MLX ViT-g probe passed: `cache_mac_m2_verification/mlx_vjepa_vitg/features.npy` shape `[2, 1408, 1]`, finite, reference parity recorded as passed.
- TRIBE c3 video smoke passed: `cache_mac_m2_verification/mlx_vjepa_tribe_c3.json`, predictions `[11, 20484]`, finite.
- TRIBE c1 audio+video smoke passed: `cache_mac_m2_verification/mlx_vjepa_tribe_c1_video_audio.json`, Audio+Video events, predictions `[11, 20484]`, finite, `video_blocker=false`.
- Notebook modes passed/refused as intended: safe and `mlx_vjepa_probe` execute; `mlx_vjepa_predict` refuses without `TRIBEV2_MLX_VJEPA_PREDICT_OPT_IN=1`.
- Final audit artifact: `.omx/state/vjepa-mlx-final-audit.json`.
- Final architect review: approved (`019e262e-83e7-7c90-ae18-abd4133114a6`).

The original full-success claim remains historical evidence for finite real ViT-g MLX features consumed by TRIBE c3/c1 smoke paths, but its fp16-derived reference caveat is superseded by the float32 correctness completion addendum below. Future optimization must not replace this with tiny, non-ViT-g, or lower-precision outputs unless separately parity-proven against the float32 correctness lane.

## Ralph dtype-correctness addendum (2026-05-14)

The follow-up correctness lane resolves the fp16 ambiguity left by the first final audit:

- Correctness preflight must record `correctness_policy=float32`, `runtime_dtype=float32`, and `selected_policy.dtype=float32`.
- Correctness mapping must use `cache_mac_m2_verification/vjepa_mlx_weight_mapping_float32.json` and record float32 provenance.
- Correctness reference must use `cache_mac_m2_verification/vjepa_reference_float32/` with `hidden_states_dtype=float32`.
- Correctness aggregation must record float32 hidden-state source dtype.
- Correctness probe metadata must reference both the float32 mapping artifact and `cache_mac_m2_verification/vjepa_reference_float32/aggregated_features.npy`.
- The older fp16-derived reference/mapping artifacts remain historical/compact evidence only and must fail closed if used as the primary success path.

## Ralph float32 correctness completion addendum (2026-05-14)

The dtype/provenance follow-up is complete:

- Float32 preflight: `cache_mac_m2_verification/vjepa_vitg_memory_preflight.json` records `correctness_policy=float32`, `runtime_dtype=float32`, and `selected_policy.dtype=float32`.
- Float32 mapping: `cache_mac_m2_verification/vjepa_mlx_weight_mapping_float32.json` records `dtype_policy=float32` and `artifact_role=correctness`.
- Float32 HF reference: `cache_mac_m2_verification/vjepa_reference_float32/reference_features.npz` and `reference_report.json` record `hidden_states_dtype=float32`.
- Float32 aggregation: `cache_mac_m2_verification/vjepa_reference_float32/aggregated_features.npy` has shape `[2, 1408, 1]` and finite values.
- MLX ViT-g probe: `cache_mac_m2_verification/mlx_vjepa_vitg/metadata.json` references the float32 mapping and float32 aggregate; parity passed with cosine `0.9836995107` and mean absolute error `0.9371865491` under the recorded thresholds.
- TRIBE c3 and c1 audio+video smokes still complete with finite `[11, 20484]` predictions.

The prior full-audit caveat about fp16-saved HF hidden-state reference parity is resolved for the primary correctness lane. fp16/bf16 remain future optimization-only work and must not overwrite the float32 correctness artifacts.
